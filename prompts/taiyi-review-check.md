---
description: "TaiyiForge /taiyi:review-check — 单次机器审查 REVIEW.md（不计轮次）"
argument-hint: "<slug>"
---
User invoked **$taiyi-review-check** (= `/taiyi:review-check`). Run:

```bash
scripts/taiyi-forge.sh review-check $ARGUMENTS
```

Read-only machine gate probe. For iterative review until pass, use `/taiyi:review-loop` instead.
