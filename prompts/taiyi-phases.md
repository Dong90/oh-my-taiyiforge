---
description: "TaiyiForge /taiyi:phases — list nine phases, skills, artifacts"
argument-hint: "(no args)"
---
User invoked **$taiyi-phases** (= `/taiyi:phases`). Run:

```bash
scripts/taiyi-forge.sh phases
```

Prints phase registry (Skill id, artifact path, dependencies). For current change progress use `/taiyi:status`.
