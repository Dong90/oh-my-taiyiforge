---
description: "TaiyiForge /taiyi:sciomc — 科研/实验工作流（对标 OMC sciomc）"
argument-hint: "[slug]"
---
User invoked **$taiyi-sciomc** (= `/taiyi:sciomc`).

```bash
scripts/taiyi-forge.sh sciomc $ARGUMENTS
```

假设 → spike → 证据 → `/taiyi:ralph` → 回写 DESIGN/REQUIREMENT。
