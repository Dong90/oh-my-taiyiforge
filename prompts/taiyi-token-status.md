---
description: "TaiyiForge /taiyi:token status — Token 用量 / 预算 / 压缩建议"
argument-hint: "optional slug"
---
User invoked **$taiyi-token-status** (= `/taiyi:token status`). Run:

```bash
scripts/taiyi-forge.sh token status $ARGUMENTS
```

Shows token usage, budget %, phase limits, and compress suggestions when over threshold.
