---
description: "TaiyiForge /taiyi:feature — new feature playbook (full nine phases)"
argument-hint: "[title or slug]"
---
User invoked **$taiyi-feature** (= `/taiyi:feature`). **Full feature scenario** — nine phases, default profile full.

```bash
scripts/taiyi-forge.sh feature $ARGUMENTS
```

If no change yet: `/taiyi:new <标题>` then follow playbook. Each phase: **`/taiyi:write`** → load Skill from engine output. Overview: `/taiyi:flow`.

{{TAIYI_STAGE_PROTOCOL}}
