---
description: "TaiyiForge /taiyi:archive — archive completed change (OpenSpec opsx:archive)"
argument-hint: "optional slug"
---
User invoked **$taiyi-archive** (= `/taiyi:archive`). Run:

```bash
scripts/taiyi-forge.sh archive $ARGUMENTS
```

Only after nine phases complete. Omit slug when only one active change.
