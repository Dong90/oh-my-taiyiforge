---
description: "TaiyiForge /taiyi:ci platform — smoke-test skill install for one platform"
argument-hint: "<opencode|claude|codex|cursor>"
---
User invoked **$taiyi-ci-platform** (= `/taiyi:ci platform`). Run:

```bash
scripts/taiyi-forge.sh ci platform $ARGUMENTS
```

Maintainer/CI: verify taiyi-* skills sync for one platform. Not daily workflow.
