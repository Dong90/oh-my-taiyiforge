---
description: "TaiyiForge /taiyi:external-context — 外部文档/研究上下文（对标 OMC external-context）"
argument-hint: "[slug]"
---
User invoked **$taiyi-external-context** (= `/taiyi:external-context`).

```bash
scripts/taiyi-forge.sh external-context $ARGUMENTS
```

WebSearch / 官方文档 → 写 `external-context.md`（URL + 日期 + 对 AC 影响）→ `/taiyi:continue`。
