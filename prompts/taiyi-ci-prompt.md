---
description: "TaiyiForge /taiyi:ci prompt — generate CI agent prompt file for a change"
argument-hint: "<slug>"
---
User invoked **$taiyi-ci-prompt** (= `/taiyi:ci prompt`). Run:

```bash
scripts/taiyi-forge.sh ci prompt $ARGUMENTS
```

Writes CI prompt file under `.taiyi/` for automated agents. Maintainer/CI use.
