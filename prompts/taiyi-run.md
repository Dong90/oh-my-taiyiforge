---
description: "TaiyiForge /taiyi:run — onboarding walkthrough (like omc.sh run demo)"
argument-hint: "optional --slug name"
---
User invoked **$taiyi-run**. Run: `scripts/taiyi-forge.sh run $ARGUMENTS`

**Onboarding only** — runs doctor + init demo + prints next step. **Does not run all nine phases.**

Full E2E nine-phase demo: `examples/minimal-project` → `npm run walkthrough-e2e` (alias `walkthrough`).

Daily work uses new → status → continue → apply → archive.
