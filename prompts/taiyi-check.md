---
description: "TaiyiForge /taiyi:check — auto harness checklist (iron triangle → aux → main)"
argument-hint: "optional slug"
---
User invoked **$taiyi-check** (= `/taiyi:check`). Run:

```bash
scripts/taiyi-forge.sh check $ARGUMENTS
```

Shows harness plan for `--auto` changes. Use before each `/taiyi:continue` in auto mode.
