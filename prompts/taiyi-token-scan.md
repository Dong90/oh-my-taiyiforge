---
description: "TaiyiForge /taiyi:token scan — 扫描工件 Markdown Token"
argument-hint: "<slug>"
---
User invoked **$taiyi-token-scan** (= `/taiyi:token scan`). Run:

```bash
scripts/taiyi-forge.sh token scan $ARGUMENTS
```

Estimates tokens in change artifacts and records a scan entry.
