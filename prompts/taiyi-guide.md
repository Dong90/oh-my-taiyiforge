---
description: "TaiyiForge /taiyi:guide — detailed JSON guide for a change"
argument-hint: "<slug>"
---
User invoked **$taiyi-guide** (= `/taiyi:guide`). Run:

```bash
scripts/taiyi-forge.sh guide $ARGUMENTS --json
```

Machine-readable guide (quality hints, harness, sync actions). Humans prefer `/taiyi:status`.

{{TAIYI_STAGE_PROTOCOL}}
