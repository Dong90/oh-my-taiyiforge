---
description: "TaiyiForge /taiyi:token compress — 引擎压缩 → CONTEXT-COMPACT.md"
argument-hint: "<slug>"
---
User invoked **$taiyi-token-compress** (= `/taiyi:token compress`). Run:

```bash
scripts/taiyi-forge.sh token compress $ARGUMENTS
```

Writes `CONTEXT-COMPACT.md` (zero LLM). Then load **taiyi-compress** or Superpowers/gstack Skills if still over budget.
