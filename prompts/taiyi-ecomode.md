---
description: "TaiyiForge /taiyi:ecomode — 省 token 模式"
argument-hint: "[slug]"
---
User invoked **$taiyi-ecomode** (= `/taiyi:ecomode`).

```bash
scripts/taiyi-forge.sh ecomode $ARGUMENTS
```

Prefer lite profile, serial agents, `/taiyi:token compress` when over budget.
