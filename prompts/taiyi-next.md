---
description: "TaiyiForge /taiyi:next — read next step only (legacy; does not complete)"
argument-hint: "[slug]"
---
User invoked **$taiyi-next** (= `/taiyi:next`). Run:

```bash
scripts/taiyi-forge.sh next $ARGUMENTS
```

Read-only next step. Prefer `/taiyi:status` or `/taiyi:continue` to advance.

{{TAIYI_STAGE_PROTOCOL}}
