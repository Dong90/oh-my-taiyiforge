{
  "title": "Docker 容器化 + Compose + CI/CD + 环境隔离",
  "changelog_entries": [
    { "type": "feat", "description": "Dockerfile — Python 3.11-slim FastAPI 镜像带健康检查" },
    { "type": "feat", "description": "docker-compose.yml — backend (8000) + frontend Nginx (8080)" },
    { "type": "feat", "description": "nginx.conf — 反向代理 + SSE 流式支持" },
    { "type": "ci", "description": ".github/workflows/translation-assistant-ci.yml — lint + pytest + build" },
    { "type": "docs", "description": ".env.example — 环境变量模板（dev/staging/prod）" }
  ]
}
