{
  "name": "{{#if app_name}}{{app_name}}{{else}}taiyi-generated{{/if}}",
  "version": "{{#if version}}{{version}}{{else}}1.0.0{{/if}}",
  "description": "{{#if description}}{{description}}{{else}}Generated by TaiyiForge auto-plan{{/if}}",
  "scripts": {
    "start": "cd backend && uvicorn app.main:app --reload --port 8000",
    "test": "cd backend && pytest tests/ -v"
  },
  "dependencies": {}
}
