---
name: 🐛 Bug 报告
about: 报告 TaiyiForge 自身的问题（CLI 报错、Skill 失效、文档错漏等）
title: "[bug] "
labels: ["bug", "needs-triage"]
assignees: []
---

## 环境

- **OS**: (macOS 14 / Ubuntu 22.04 / Windows 11 …)
- **Node**: `node -v` 输出
- **TaiyiForge 版本**: `npx taiyi --version` 或 `git rev-parse HEAD`
- **安装方式**: 源码 / npm（v0.24+）
- **Harness**: OpenCode / Claude / Codex / Cursor / 无
- **触发的命令**: `npx taiyi <subcommand> <args>`

## 复现步骤

1.
2.
3.

## 期望行为

## 实际行为

```
<粘贴报错 / 异常输出>
```

## 关联上下文

- **相关 Issue / PR**:
- **`taiyi doctor --json --compact` 输出**（如适用）:
- **`taiyi audit --json --compact` 输出**（如适用）:
- **最小复现仓库**（如可）:

## 影响面

- [ ] 阻塞日常工作
- [ ] 文档与实际行为不一致
- [ ] 性能 / 资源异常
- [ ] 平台特定（请注明 harness + OS）
- [ ] 仅是 cosmetic / typo
